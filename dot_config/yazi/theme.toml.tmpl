# generated by chezmoi from .chezmoidata/palette.toml — edit that, not this
#
# yazi is the only consumer that switches variant by itself: it picks `dark` or
# `light` from the terminal's own background, so both flavours ship and it
# follows macOS appearance the way WezTerm and Neovim do.
[flavor]
dark  = "dopamine-dark"
light = "dopamine-light"
