// dopamine-dotfiles zellij config
// keybinds are stock zellij (your muscle memory survives);
// stacked panes: Alt-[ / Alt-] cycle swap layouts, Ctrl-p + arrows as usual.

// Ctrl-h is zellij's default "move" mode trigger, which steals it from nvim
// (Ctrl-j/k/l are never bound by zellij). Free it, park move mode on Alt-m,
// and make Alt-hjkl the within-zellij focus movement.
keybinds {
    shared_except "locked" {
        unbind "Ctrl h"
        bind "Alt m" { SwitchToMode "move"; }
        bind "Alt h" { MoveFocusOrTab "left"; }
        bind "Alt l" { MoveFocusOrTab "right"; }
        bind "Alt j" { MoveFocus "down"; }
        bind "Alt k" { MoveFocus "up"; }

        // Alt-t: bring the checklist up, and put it away again.
        //
        // Ticket sessions declare `☑ todo` as a floating pane (see the ticket
        // layout), so this only ever shows and hides what is already there —
        // it never spawns anything. Binding the key to `Run "tk" "todo"` looks
        // simpler and is wrong: zellij's Run opens a *new* pane every time it
        // fires, so each press stacked another copy until the floating layer,
        // and Alt-f with it, was unusable.
        //
        // Alt-f keeps its stock meaning. With the checklist as your only
        // floating pane the two do the same thing — Alt-t is just the mnemonic
        // that doesn't make you remember which layer you're in.
        bind "Alt t" { ToggleFloatingPanes; }
    }
}

theme "dopamine"
default_layout "dash"          // plain `zellij` = your dashboard home session
pane_frames true               // the little outline around each pane
scrollback_editor "nvim"       // Ctrl-s e → scrollback in nvim, vim motions on output
copy_on_select true
mouse_mode true
session_serialization true     // half-finished tickets survive reboots
styled_underlines true
show_startup_tips false        // no "Learn how to organise your desktop" popup

ui {
    pane_frames {
        rounded_corners false
        hide_session_name false
    }
}
