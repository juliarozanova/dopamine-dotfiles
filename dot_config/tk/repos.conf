# tk: default repo per Jira project prefix — "PREFIX  /path/to/checkout"
# Tickets without a mapping trigger a one-time fzf pick over ~/Dashboard/Code
# (remembered per-ticket afterwards in ~/.local/state/tk/repo.map).
#
# FRD   ~/Dashboard/Code/fraud-models
# HDL   ~/Dashboard/Code/hdl
